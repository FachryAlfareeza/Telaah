# Copy to .env. Never put secrets in VITE_* variables.
OPENAI_API_KEY=
# A model available to your account that supports PDF input and structured outputs.
OPENAI_MODEL=
REFERENCE_DB=./data/references.sqlite
